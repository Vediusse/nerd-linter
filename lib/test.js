// Test.js

function add(a, b) {
    var sum = a + b;
    // Пропущен return
}

function subtract(a, b) {
    // Пропущен return
    return; // Это правильно, но ничего не возвращает
}

function multiply(a, b) {
    return a * b;  // Это правильно
}

function divide(a, b) {
    if (b === 0) {
        console.log("Cannot divide by zero");
        // Пропущен return
    } else {
        return a / b;
    }
}

// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
// Неиспользуемая переменная
let unusedVariable = 42; // Неиспользуемая переменная

// Вызов функций
console.log(add(5, 10));
console.log(divide(5, 0));